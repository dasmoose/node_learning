Event listener
-- the code that responds to an event
-- the listener will be a function
-- when the event happens all functions will be envoked

// Check if something exists, else initialize it
var moo['type'] = moo['type'] || [];


// Emitter object that is created
{
 eventType: [
   function () {
    // listener code
   },
   function () {
    // listener code
   }
 ]
}
